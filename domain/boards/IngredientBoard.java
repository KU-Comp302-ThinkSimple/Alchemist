package domain.boards;

public class IngredientBoard extends Board{

    public IngredientBoard(int size, String name) {
        super(size, name);
    }
   //TODO Create a deck that is implemented with arraylist, and dynamically fill this deck with the ingredient cards created in GameInventory class so that the deck becomes endless.
    
}
